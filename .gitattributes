# Shell scripts require LF line endings.
# CRLF breaks bash execution (e.g. "syntax error near unexpected token `$'{\r''"),
# which otherwise happens on Windows when core.autocrlf=true checks out .sh as CRLF.
*.sh text eol=lf
