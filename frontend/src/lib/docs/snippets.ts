/** Shell command strings for docs code blocks (kept out of JSX to satisfy react/no-unescaped-entities). */
export const DOCS_SNIPPETS = {
  pipInstallMcp: "pip install pokt-agent-mcp",
  pipInstallMcpApi: 'pip install "pokt-agent-mcp[api]"',
} as const;