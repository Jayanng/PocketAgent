/** Stub for MetaMask SDK optional React Native dependency (web builds). */
const AsyncStorage = {
  getItem: async () => null,
  setItem: async () => undefined,
  removeItem: async () => undefined,
};

export default AsyncStorage;