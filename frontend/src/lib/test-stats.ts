/**
 * Published test counts — keep in sync with README.md Testing section.
 * Run `cd frontend && npm test` and `cd backend && pytest -q` to verify.
 */
export const TEST_STATS = {
  total: 252,
  backend: 175,
  frontend: 77,
} as const;